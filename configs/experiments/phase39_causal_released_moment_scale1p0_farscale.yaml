experiment_name: phase39_causal_released_moment_scale1p0_farscale
# Knob 5b on top of NEW-3 only (seed 73, ΔMw near [-1.0, 0.5]).
# Not stacked on event-balanced sampling, so the distance prior can be
# isolated from 5a.
fixed_endpoint_candidate_summary: /home/lihe/PINN_Mag/runs/phase39-expanded-fixed-split-20260831T0740Z/candidates/seed_73/candidate_summary.json
expected_split_assignment_sha256: e4807aa1e6b5b389caf23974f62ff9da6b8add7f7887ec23a59d5d35a455eba7
fold: 0
seed: 73
training:
  initialization: scratch
  trainable_scope: all
  epochs: 200
  learning_rate: 1.0e-04
  weight_decay: 1.0e-05
  grad_clip_norm: 1.0
  early_stop_patience: 50
  warmup_epochs: 5
  scheduler_T0: 15
  scheduler_T_mult: 2
  scheduler_eta_min: 1.0e-06
  minimum_prefix_horizon_sec: 5
  maximum_prefix_horizon_sec: 199
  horizon_cycle_multiplier: 73
  prefix_pair_gap_sec: 0
  prefix_curriculum_start_epoch: 0
  prefix_curriculum_full_epoch: 1
  prefix_science_weight: 0.5
  endpoint_science_weight: 1.0
  lambda_synth: 0.5
  moment_scale_augmentation_weight: 1.0
  moment_scale_delta_min_mw: -1.0
  moment_scale_delta_max_mw: 0.5
  # Knob 5b: far stations draw more negative ΔMw so a Tohoku 300 km
  # record can become an M7.6 far-field example.
  moment_scale_distance_stratified: true
  moment_scale_near_km: 100.0
  moment_scale_far_km: 300.0
  moment_scale_far_delta_min_mw: -1.5
  moment_scale_far_delta_max_mw: 0.0
  prefix_presentation: zero_pad
  released_moment_floor_nm: 1.0e+15
  min_constrained_window_sec: 1.0
  error_descent_weight: 0.0
  released_monotone_weight: 0.0
  released_monotone_slack_mw: 0.03
evaluation:
  anchor_horizons_sec:
    - 30
    - 60
    - 90
    - 120
    - 160
    - 200
  full_horizon_start_sec: 1
  full_horizon_end_sec: 200
  endpoint_target_mw: 0.171305
  endpoint_stretch_target_mw: 0.127016
  endpoint_band_tolerance_mw: 0.05
  processing_delay_sec: 6.0
